<div class="container">
<h1>Edit potin n° <%= params[:id] %></h1>
<h2><sub>No efforts on the front, sorry i was sleeping yesterday</sub></h2>
<p><%= form_for @gossip do |k| %>

    <input type='hidden' name='authenticity_token' value='<%= form_authenticity_token %>'>

  <div class='form-group'>
  <label for='title'>Titre</label>
  <%= k.text_field :title, class: "form-control" %>
  </div>

  <div class="form-group">
    <label for="exampleTextarea">Corps du potin</label>
    <%= k.text_area :text, class: "form-control" %>
  </div>

<button type="submit" class="mr-5 btn btn-primary">Submit</button>

<% end %></p>
</div>
